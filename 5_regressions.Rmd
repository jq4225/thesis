---
title: "5_regressions"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(lfe)
library(texreg)
library(fixest)
```


```{r}
# read in searches v4 or later

test <- felm(I(log(depression)) ~  sip_first * year_indicator + 
               cases_per_100k + deaths_per_100k + unemploy_rate|dma + week|0|dma, 
             data = searches_v4)

test2 <- feols(I(log(depression)) ~  sip_first * year_indicator + 
               cases_per_100k + deaths_per_100k + unemploy_rate|dma + week, 
               cluster = ~ dma,
               data = searches_v4)
```


